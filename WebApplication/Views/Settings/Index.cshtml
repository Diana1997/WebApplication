@model IEnumerable<WebApplication.Models.Setting>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HairSizeSettings.HairSizeSettingsID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StatisticalSettings.StatisticalSettingsID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HairSizeSettings.HairSizeSettingsID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StatisticalSettings.StatisticalSettingsID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SettingsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SettingsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SettingsID })
        </td>
    </tr>
}

</table>
