@model IEnumerable<WebApplication.Models.CommentOnTheVisit>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeOfComment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comment)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeOfComment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comment)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CommentOnTheVisitID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CommentOnTheVisitID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CommentOnTheVisitID })
        </td>
    </tr>
}

</table>
